version: '3'

services:
  db:
    build: ./postgrdb
    ports:
      - "5432:5432"
    networks:
      - mynetwork

  mainapp:
    build: .
    volumes:
      - .:/app
      - ./logs:/app/logs
    depends_on:
      - db
    networks:
      - mynetwork
    environment:
      TZ: Europe/Moscow

networks:
  mynetwork: {}